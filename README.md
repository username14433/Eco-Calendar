# 🌿 **Eco Calendar**  
Веб-приложение на Java/Spring для просмотра и администрирования экологических событий

---

# 🎓 **Eco Calendar — Spring Boot Practice Project**

## 📘 **Описание проекта**  
**Eco Calendar** — это учебно-практический веб-проект, демонстрирующий работу с **Spring Boot**, **Spring Security**, **Hibernate ORM**, **Docker** и **PostgreSQL**.  
Приложение представляет собой онлайн-календарь экологических событий с разделением пользователей на роли **User** и **Admin**.

- 👤 **User** — может просматривать события  
- 🛡 **Admin** — имеет полный доступ к CRUD-операциям: добавлять, изменять и удалять события  

Данные передаются через **REST API**, а пользовательские представления формируются при помощи шаблонизатора **Thymeleaf**.  
Приложение полностью контейнеризовано с использованием **Docker Compose** + **Docker Volumes**, что обеспечивает сохранность данных между перезапусками.

---

## ⚙️ **Используемые технологии**

| Технология | Назначение |
|------------|------------|
| ☕ **Java 17+** | Основной язык разработки |
| 🍃 **Spring Boot** | Базовый каркас приложения |
| 🔐 **Spring Security** | Авторизация, аутентификация, разграничение ролей |
| 🧩 **Spring Data JPA** | Высокоуровневая работа с БД через JPA |
| 🐘 **PostgreSQL** | Реляционная база данных |
| 🧱 **Hibernate ORM** | ORM-движок для работы с сущностями |
| 🐳 **Docker + Docker Compose + Volumes** | Контейнеризация и сохранение данных |
| 🌿 **Thymeleaf** | Серверный шаблонизатор (HTML) |
| 📦 **Maven** | Сборка и управление зависимостями |

---

## 🏗️ **Архитектура проекта**

src/  
└── main/  
├── java/  
│ └── org.example.ecocalendar/  
│ ├── controller/ // MVC и REST контроллеры  
│ ├── service/ // Бизнес-логика  
│ ├── repository/ // JPA-репозитории  
│ ├── model/ // Сущности Hibernate (Event, User и т.д.)  
│ └── security/ // Конфигурация Spring Security  
│  
└── resources/  
├── templates/ // HTML-шаблоны (Thymeleaf)  
└── static/ // CSS, JS, изображения  


## 🧩 **Основные сущности приложения**

| Сущность | Описание |
|----------|----------|
| **Event** | Экологическое событие (название, дата, описание) |
| **User** | Пользователь системы |
| **Role** | Роли пользователей (`USER`, `ADMIN`) |
| **Event DTO** | Объект для передачи данных через REST API |

> Проект использует связку Spring Security + Hibernate, где каждый пользователь имеет одну или несколько ролей.

---

## 🔐 **Роли и доступы**

| Роль | Возможности |
|------|-------------|
| **USER** | Просмотр событий |
| **ADMIN** | Добавление, редактирование, удаление событий (полный CRUD) |

Защита доступа реализована через **Spring Security**, включая:

- ограничение admin-маршрутов  
- авторизацию формой входа  
- фильтрацию запросов  

---

## 🌐 **REST API + Thymeleaf**

Приложение сочетает:

- **REST API** для обмена данными  
- **Thymeleaf** для отрисовки HTML-страниц  

---

## 🐳 **Работа с базой данных через Docker**

Для PostgreSQL используется Docker Compose + Volume — это обеспечивает постоянство данных.

### Пример запуска:

```bash
docker compose up --build
Приложение становится доступно по адресу:

arduino
Копировать код
http://localhost:8080
🚀 Запуск проекта вручную
1. Клонируйте репозиторий:
bash
Копировать код
git clone https://github.com/username/Eco-Calendar.git
cd Eco-Calendar
2. Запустите PostgreSQL через Docker (если без compose):
bash
Копировать код
docker volume create eco_calendar_volume
docker run --name eco_calendar_postgres \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=eco_calendar \
  -v eco_calendar_volume:/var/lib/postgresql/data \
  -p 5432:5432 \
  -d postgres:latest
3. Соберите и запустите приложение:
bash
Копировать код
mvn clean install
mvn spring-boot:run


🧾 Навыки, продемонстрированные в проекте
Работа с Spring Boot

Построение MVC-архитектуры

Реализация Spring Security и ролевой модели

Создание CRUD-функционала

Настройка Hibernate ORM и аннотаций JPA

Создание REST API

Работа с PostgreSQL через Hibernate

Контейнеризация приложения с Docker

Управление зависимостями через Maven

👨‍💻 Автор
<PRIVATE_PERSON>
💬 Java Developer | Spring | Hibernate | SQL | Docker | PostgreSQL
